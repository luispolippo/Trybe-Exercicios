const toUpperCase = (string) => string.toUpperCase();

const firstLetter = (string) => string[0];

const stringConcat = (string1, string2) => string1.concat(string2);

module.exports = { toUpperCase, firstLetter, stringConcat };
